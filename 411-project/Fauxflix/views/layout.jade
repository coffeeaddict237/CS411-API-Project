include logo.html
doctype html

head
    block stylesheets
        link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css')
        link(rel='stylesheet', href='../stylesheets/style.css')
    block jscripts
        script(src='http://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js')
    title Fauxflix
body
    html
        body

            p(style='color: red')
                strong #{errormsg}
            p(style='color: black')
                strong #{signinSuccess}
            p(style='color: black')
                strong #{loginSuccess}
            p Step 1

                p Can't decide what to watch? Let us help!

                    form(id = 'city-form', action='/search', method='POST', style = 'text-align:center;')
                        //- console.log("Someone is searching for the weather");
                        div.form-group
                            //label City
                            input#city.form-control(type = 'text', placeholder = 'Enter your current city', name = 'city')
                        div.form-group
                            //label City
                            input#state.form-control(type = 'text', placeholder = 'Enter your current state', name = 'state')
                        button.btn.btn-primary(type = 'submit') Enter

                        p #{weather}
                        p #{conditions}

                // code for second API search
            p Step 2

                p Great, now that we have your current city and weather, let's find you the perfect movie!


                    form(id = 'movie-button', action = '/generate', method = 'POST', style = 'text-align:center;') If you would like to watch a movie generated by your weather, just click the generator button here:
                        div.form-group
                            //label City
                            input#conditions.form-control(type = 'text', placeholder = 'Enter the keyword above', name = 'conditions')
                        button.btn.btn-primary(type = 'submit') Go!

                        p #{keyword}
                        img(src="#{poster}")

                        p(style='white-space: pre;') #{movies}

                    p If you would like to watch a movie generated by a specific genre that isn't related to your weather conditions, choose one from the drop down menu below
                        include menu.html
                    link(rel='stylesheet', href='../stylesheets/style.css')

            h3
              p Would you like to login with Facebook or Twitter so we can save your movie picks?
                form(id='sign-up', action='/register', style='text-align: center', method='POST')
                    .container By creating an account you agree to our
                        a(href='/copyrights')  Terms & Privacy
                        .row
                            input(type='text', placeholder='Enter Username', name='username')
                            input(type='text', placeholder='Enter Password', name='password')
                            input(type='text', placeholder='Repeat Password', name='password2')
                        button.btn.btn-primary(type = 'submit') Submit

                form(id='log-in', action='/login', style='text-align: center', method='POST') Log in here
                    .container
                        .row
                            input(type='text', placeholder='Username', name='username')
                            input(type='text', placeholder='Password', name='password')
                        button.btn.btn-primary(type = 'submit') Submit


        p Find our project repo
          a(href='https://github.com/coffeeaddict237/CS411-API-Project')  here





